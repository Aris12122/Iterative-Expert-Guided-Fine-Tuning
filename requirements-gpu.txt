# Requirements for GPU training in Yandex Cloud
# Install PyTorch with CUDA support first, then install other dependencies

# PyTorch with CUDA 11.8 (for most GPUs)
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Or for CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Core ML libraries
transformers>=4.30.0
datasets>=2.14.0
numpy>=1.24.0
tqdm>=4.65.0

# Optional: for better performance
# accelerate>=0.20.0  # For distributed training
# tensorboard>=2.13.0  # For visualization

