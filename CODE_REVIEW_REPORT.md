# Отчет о проверке кода: Почему accuracy ~0.25?

## ✅ Проверка кода

### 1. Загрузка данных - **РАБОТАЕТ ПРАВИЛЬНО**
- ✅ Метки загружаются корректно из поля `cop`
- ✅ Нормализация меток работает (0-индексирование)
- ✅ Распределение меток нормальное: {0, 1, 2, 3} примерно равномерно
- ✅ Нет проблем с отсутствующими метками

### 2. Модель и forward pass - **РАБОТАЕТ ПРАВИЛЬНО**
- ✅ Reshape из плоского батча в формат multiple choice работает корректно
- ✅ Forward pass возвращает logits правильной формы (batch_size, num_options)
- ✅ Loss вычисляется правильно (cross-entropy)

### 3. Обучение - **КОД ПРАВИЛЬНЫЙ, НО НАСТРОЙКИ НЕДОСТАТОЧНЫ**
- ✅ Оптимизатор настроен правильно (AdamW)
- ✅ Scheduler работает (LinearLR с warmup)
- ✅ Gradient clipping применяется
- ⚠️ **ПРОБЛЕМА**: Настройки слишком консервативные для обучения

## 🔍 Причина низкой accuracy

### Главная проблема: **Недостаточно данных и эпох для обучения**

Текущие настройки в `default_medqa_experiment()`:
```python
max_samples=500      # ❌ Слишком мало! Всего 500 примеров
num_epochs=1         # ❌ Слишком мало! Нужно минимум 3-5
batch_size=8         # ⚠️ Маленький, но приемлемо для CPU
warmup_steps=10      # ❌ Слишком мало! Нужно 100-500
```

**Почему это проблема:**
1. **500 примеров** - после разбиения на train/dev остается ~400 примеров для обучения
   - Для BERT/DistilBERT нужно минимум 1000-5000 примеров для нормального обучения
   - С 500 примерами модель не может выучить паттерны

2. **1 эпоха** - модель не успевает обучиться
   - Нужно минимум 3-5 эпох для сходимости
   - За 1 эпоху модель только начинает "понимать" задачу

3. **warmup_steps=10** - слишком мало для стабильного обучения
   - Learning rate не успевает правильно прогреться
   - Может привести к нестабильному обучению

## 📊 Ожидаемые результаты

### С текущими настройками (500 примеров, 1 эпоха):
- Accuracy: **~0.25-0.30** (случайное угадывание)
- Loss: **~1.3-1.4** (не уменьшается)
- **Вывод**: Модель практически не обучается

### С правильными настройками (полный датасет, 5 эпох):
- Accuracy: **0.40-0.60+** (зависит от сложности задачи)
- Loss: **<1.0** (уменьшается с ~1.4 до <1.0)
- **Вывод**: Модель обучается нормально

## ✅ Решение

### Вариант 1: Быстрый тест (для проверки кода)
```bash
python3 scripts/train_supervised.py \
    --experiment_name "test_improved" \
    --max_samples 2000 \
    --batch_size 16 \
    --num_epochs 3 \
    --warmup_steps 200
```
**Ожидаемый результат**: accuracy ~0.35-0.45

### Вариант 2: Полное обучение (для реальных результатов)
```bash
python3 scripts/train_supervised.py \
    --experiment_name "supervised_full" \
    --max_samples None \
    --batch_size 32 \
    --num_epochs 5 \
    --warmup_steps 500
```
**Ожидаемый результат**: accuracy ~0.50-0.65

### Вариант 3: Использовать улучшенную конфигурацию
В коде уже есть функция `improved_medqa_experiment()` в `src/config.py`:
```python
from src.config import improved_medqa_experiment

config = improved_medqa_experiment(
    experiment_name="improved",
    experiment_type="supervised",
    use_full_dataset=True,
    gpu_optimized=False,  # True для GPU
)
```

## 🔧 Добавленные улучшения

1. **Расширенное логирование** в `src/training/supervised.py`:
   - Логирование уменьшения loss
   - Предупреждения о низкой accuracy
   - Проверка, что модель не предсказывает только один класс

2. **Диагностический скрипт** `scripts/diagnose_data.py`:
   - Проверка загрузки данных
   - Проверка работы модели
   - Тестовое обучение

## 📝 Рекомендации

1. **Для быстрой проверки кода**: Используйте `max_samples=2000`, `num_epochs=3`
2. **Для реальных результатов**: Используйте `max_samples=None`, `num_epochs=5`
3. **Мониторинг обучения**: Проверяйте логи - loss должен уменьшаться, accuracy должна расти
4. **Если accuracy все еще низкая**: 
   - Увеличьте количество эпох до 10
   - Попробуйте другую модель (bert-base вместо distilbert)
   - Проверьте, что данные действительно загружаются правильно

## ✅ Вывод

**Код работает правильно!** Проблема не в коде, а в настройках:
- Слишком мало данных (500 примеров)
- Слишком мало эпох (1 эпоха)
- Слишком мало warmup_steps (10)

**После увеличения этих параметров accuracy должна значительно улучшиться.**

